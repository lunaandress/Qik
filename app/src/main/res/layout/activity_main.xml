<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="24dp"
    android:background="@color/white">

    <!-- Logo con un tamaño más adaptable -->
    <ImageView
        android:id="@+id/logoImageView"
        android:layout_width="120dp"
        android:layout_height="120dp"
        android:src="@drawable/logo"
        android:contentDescription="@string/logo_desc"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="48dp"
        android:scaleType="centerInside" />

    <!-- Texto de bienvenida con fuente Roboto y estilo Material -->
    <TextView
        android:id="@+id/welcomeTextView"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Bienvenido a App Qik"
        android:textSize="28sp"
        android:textStyle="bold"
        android:fontFamily="@font/roboto_slab"
        android:textColor="@color/black"
        android:letterSpacing="0.02"
        android:textAlignment="center"
        app:layout_constraintTop_toBottomOf="@id/logoImageView"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="24dp" />

    <!-- Botón Material con sombra, ripple y borde redondeado -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnViewMenu"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="Ver Carta"
        android:textAllCaps="false"
        android:textColor="@color/white"
        app:backgroundTint="@color/black"
        app:cornerRadius="24dp"
        android:fontFamily="@font/roboto_medium"
        android:textSize="18sp"
        app:iconGravity="textStart"
        app:rippleColor="@color/yellow_500"
        android:elevation="6dp"
        app:layout_constraintTop_toBottomOf="@id/welcomeTextView"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        android:layout_marginTop="48dp"
        android:layout_marginHorizontal="32dp" />

</androidx.constraintlayout.widget.ConstraintLayout>
